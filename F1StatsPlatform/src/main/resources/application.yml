spring:
  application:
    name: f1-stats-platform

  datasource:
    url: jdbc:postgresql://localhost:5432/f1platform
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:archil.tsulaia1@gmail.com}
    password: ${MAIL_PASSWORD:sioa foda wten rkhm}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com
            checkserveridentity: false

server:
  port: 8090

app:
  jwt:
    secret: ${JWT_SECRET:dev-only-secret-please-change-me-before-deploying-anywhere-real}
    expiration-ms: 86400000

  external-api:
    jolpica-base-url: https://api.jolpi.ca/ergast/f1
    openf1-base-url: https://api.openf1.org/v1

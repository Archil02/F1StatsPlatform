spring:
  application:
    name: f1-stats-platform

  datasource:
    url: jdbc:postgresql://localhost:5432/f1platform
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com
            checkserveridentity: false

server:
  port: 8090

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: 86400000

  external-api:
    jolpica-base-url: https://api.jolpi.ca/ergast/f1
    openf1-base-url: https://api.openf1.org/v1